# This query shows countries in north american continent and lists their languages and currency
query {
  country(code: "US") {
    continent {
      name
      countries {
        name
        languages {
          name
          code
        }
        currency
      }
    }
  }
}
